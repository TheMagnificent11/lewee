name: Continuous Integration

on:
  workflow_dispatch:
  push:
    branches: [ develop, main ]

permissions:
  contents: write
  pull-requests: write

jobs:
  buildAndTest:
    uses: ./.github/workflows/build-and-test.yml