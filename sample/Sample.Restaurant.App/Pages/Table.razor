@page "/tables/{TableNumber:int}"

@inject IDispatcher Dispatcher

<h1>Table @this.TableNumber</h1>

@if(this.table == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Item Name</th>
                <th>Price</th>
                <th>Quantity</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in this.table.Items)
            {
                <tr>
                    <td>@item.MenuItem.Name</td>
                    <td>@item.MenuItem.PriceLabel</td>
                    <td>@item.Quantity</td>
                    <td>
                        <button @onclick="@(() => AddToOrder(item.MenuItem.Id))">Add</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}