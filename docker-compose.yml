version: '3.4'

networks:
  lewee-network:
    name: lewee_network

services:
  sample.restaurant.app:
    image: ${DOCKER_REGISTRY-}samplerestaurantapp
    build:
      context: .
      dockerfile: sample/Sample.Restaurant.App/Dockerfile
    networks:
      - lewee-network
  seq:
    container_name: lewee_seq
    image: datalust/seq
    ports:
      - 5341:80
    environment:
      - ACCEPT_EULA=Y
    volumes:
      - ./seq:/data
    networks:
      - lewee-network
